// Intentional no-op: replaces the `server-only` Next.js build-time guard in
// Vitest's jsdom environment where the guard would otherwise throw.
// See vitest.config.mts → resolve.alias["server-only"].
