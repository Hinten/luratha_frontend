/**
 * Canonical storefront origin used to build product `g:link` / image URLs in the
 * feed. Duplicated from the storefront (`apps/store`) because workspace apps can
 * not import across each other — keep it in sync if the public domain changes.
 */
export const SITE_URL = "https://www.luratha.com.br";
