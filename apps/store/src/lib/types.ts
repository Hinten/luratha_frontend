/* @deprecated
  * These types are deprecated and should not be used in new code.
  * Import them from the corresponding schemas under "@luratha/schemas/*" instead.
*/
export type {
  Product,
} from "@luratha/schemas/products";

export type {
  Category,
} from "@luratha/schemas/category";

export type {
  Review,
} from "@luratha/schemas/reviews";
